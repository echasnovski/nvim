" Usually having `lion_squeeze_spaces = 1` disables alignment inside visual
" block (which is helpful when doing multiple consecutive alignments with
" different symbols, like `=` and `,`).
let g:lion_squeeze_spaces = 0
