name: Test 'mini.input'

on: [push]

jobs:
  build:
    name: Test 'mini.input'
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        neovim_version: ['v0.10.2', 'v0.11.5', 'v0.12.2', 'nightly']
        include:
        - os: macos-latest
          neovim_version: v0.12.2
        - os: windows-latest
          neovim_version: v0.12.2
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v5
      - name: Setup neovim
        uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: ${{ matrix.neovim_version }}
      - name: Run tests
        run: make test_input
